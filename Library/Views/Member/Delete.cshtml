@model Library.Models.Member

@{
    ViewData["Title"] = "Delete";
    var userRole = ViewBag.UserRole;
    var memberId = ViewBag.MemberId; 
}

<h1>Delete</h1>

@if (ViewBag.UserRole == "Host" || 
    (ViewBag.UserRole == "Member" && ViewBag.MemberId.HasValue && Model.MemberId == ViewBag.MemberId.Value))
{
    <h3>Are you sure you want to delete this member?</h3>
    <div>
        <h4>Member</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.MemberName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.MemberName)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Email)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Email)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.PhoneNumber)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.MembershipDate)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.MembershipDate) <!-- بدون استدعاء ToShortDateString() -->
            </dd>
        </dl>

        <form asp-action="DeleteConfirmed" method="post">
            <input type="hidden" asp-for="MemberId" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </form>
    </div>
}
else
{
    <p>You do not have permission to delete this member.</p>
}


